
1. Create the following class:
    name: Circle
    fields:
       - radius   (double)
       - color    (String)
    constructors:
       - Circle()
       - Circle(double radius)
    methods:
        getRadius()         (return values are double, returns the radius values)
        getArea()           (return value is double, return the area of the circle)

2. Create a Main class
    name: Main
    contains only the public static void main method

    Create 3 objects in this class.

    -radius 15, red circle
    -radius 16.7, blue circle
    -radius 1.5, green circle
 A: Write out their values and the area on the screen
 B: Increase double the value of the radius of the green circle
 C: Print the new area of the green circle








